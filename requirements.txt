# ─────────────────────────────────────────────────────────────────────────────
# CommunityFinanceOS — SaccoOS Platform
# requirements.txt
# Pinned versions tested on Streamlit Cloud (Python 3.11)
# ─────────────────────────────────────────────────────────────────────────────

# Core framework
streamlit>=1.35.0

# Database
# SQLite is built into Python — no package needed

# Date utilities (used in reports.py for monthly trend calculations)
python-dateutil>=2.9.0

# AI Insights (Anthropic API)
anthropic>=0.28.0

# QR code generation (for Login QR Codes page)
qrcode[pil]>=7.4.2
Pillow>=10.3.0

# PDF generation (for future report export — already in roadmap)
reportlab>=4.2.0

# Data export / manipulation
openpyxl>=3.1.2

# HTTP requests (for payment gateway integrations — MTN MoMo, Airtel, Pesapal)
requests>=2.32.0

# Environment variable management (local dev)
python-dotenv>=1.0.1
